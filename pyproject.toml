[tool.poetry]
name = "kea_config"
version = "4.5.1"
description = "Manage kea dhcp from simple conf file"
authors = ["Gene C"]
license = "MIT"
readme = "README.rst"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
